// Sidebar MANUAL, na ordem em que a doc deve ser lida.
// Autogerado ficaria em ordem de arquivo e a leitura perderia o fio: aqui a ordem
// é "o que é o jogo -> como o trabalho é feito -> qual é a régua -> como entrar".

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  dev: [
    'comecando',
    'instrumentacao-ai',
    'quality-gates',
    'colaborar',
    'arquitetura',
    'estado',
  ],
};

module.exports = sidebars;
